{{ form_start(form) }}
<div class="form-group">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            {{ form_row(form.ClFirstName) }}
        </div>
    </div>
</div>

<div class="form-group">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            {{ form_row(form.ClFamilyName) }}
        </div>
    </div>
</div>

<div class="form-group">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            {{ form_row(form.ClCountry) }}
        </div>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-md-3 offset-md-1">
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    </div>
</div>
{{ form_end(form) }}
